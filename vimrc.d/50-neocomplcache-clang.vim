" This variable whether clang_complete use clang library(clang.dll or
" libclang.so or libclang.dylib).  If this option is enabled, clang_complete
" will be faster.  But required clang library(You may compile clang manually).
" And this option requires python interface in Vim.
let g:neocomplcache_clang_use_library = 1

" This variable whether enable debug message.
let g:neocomplcache_clang_debug = 0
